# vim: ft=ruby

require 'rubygems'
require 'rubygems/gem_runner'
require 'rubygems/exceptions'

def Gem.ruby
  "/usr/bin/ruby"
end

def Gem.bindir(*args)
  "#{Dir.pwd}/debian/tmp/usr/bin"
end

args = ARGV.clone

begin
  Gem::GemRunner.new.run args
rescue Gem::SystemExitException => e
  exit e.exit_code
end
